% ============================================================
%  SYSTEM BLOCK DIAGRAM — 6 columns
% ============================================================
\begin{tikzpicture}[
    >=stealth,
    arr/.style    = {->, thick},
    arrb/.style   = {<->, thick},
    box/.style    = {rectangle, draw, rounded corners=3pt,
                     minimum width=1.8cm, minimum height=0.80cm,
                     font=\footnotesize, align=center, fill=white},
    sensor/.style = {box, fill=orange!15, draw=orange!70},
    percep/.style = {box, fill=blue!12,   draw=blue!60},
    injury/.style = {box, fill=red!10,    draw=red!55},
    fsmbox/.style = {box, fill=purple!22, draw=purple!80,
                     minimum width=1.8cm, minimum height=5.8cm},
    plan/.style   = {box, fill=cyan!12,   draw=cyan!60},
    ctrl/.style   = {box, fill=green!12,  draw=green!60!black},
    web/.style    = {box, fill=yellow!10, draw=yellow!60!black},
    hw/.style     = {box, fill=gray!15,   draw=gray!60},
  ]

  % ── Column x: 0  2.3  4.9  7.2  9.5  12.2 ─────────────────

  % SENSORS (x=0)
  \node[sensor] (orb)  at ( 0.0,  2.2) {Orbbec\\RGB-D};
  \node[sensor] (rs)   at ( 0.0, -1.6) {RealSense\\RGB-D};

  % PERCEPTION (x=2.3)
  \node[injury] (yolo) at ( 2.5,  2.2) {YOLO-Pose};
  \node[percep] (kf)   at ( 2.5,  1.0) {Kalman 3D};
  \node[injury] (inj)  at ( 2.5, -2.8) {Injury\\Detection};
  \node[percep] (surf) at ( 2.5, -1.6) {Surface\\Estimator};
  \node[injury] (nlf)  at ( 2.5, -0.3) {NLF\\Skeleton};

  % MISSION FSM (x=4.9)
  \node[fsmbox] (fsm)  at ( 5.0, -0.3) {Mission\\FSM\\(13 states)};

  % PLANNING (x=7.2)
    \node[web]  (webui)   at (7.5, -0.9) {Web UI\\(review)};
  \node[plan] (exp)     at (7.5,  0.3) {Exp. Scanner};
  \node[plan] (fast)    at (7.5,  -2.1) {FAST\\Ultrasound};
  \node[plan] (bpose)   at (7.5, 1.5) {Body Pose\\Optimizer};

  % CONTROL (x=9.5)
  \node[ctrl] (wbc)     at (10,  0.3) {WBC QP};
  \node[ctrl] (mux)     at (10,  -0.9) {IK Goal\\Mux};
  \node[ctrl] (imp)     at (10, -2.1) {Impedance\\ctrl};

  % HARDWARE (x=12.2)
  \node[hw] (spot) at (12.5,  1.5) {Spot\\(cmd\_vel, $h$,$\theta$)};
  \node[hw] (z1)   at (12.5, -0.9) {Z1 arm};

  % ── Sensors → Perception ───────────────────────────────────
  \draw[arr] (orb.east)   -- (yolo.west);
  \draw[arr] (orb.south)  |- node[above left,font=\footnotesize]{RGB} (nlf.west);
  \draw[arr] (yolo.south) -- (kf.north);
  \draw[arr] (rs.east)   -- (surf.west);
  \draw[arr] (rs.south)  |- node[above left,font=\footnotesize]{RGB} (inj.west);

  % ── Perception → FSM ───────────────────────────────────────
  \draw[arr] (kf.east)   -- node[above,font=\footnotesize]{$\hat{\mathbf{p}}$} (fsm.west |- kf);
  \draw[arr] (inj.east)  -- (fsm.west |- inj);
  \draw[arr] (surf.east) -- node[above,font=\footnotesize]{$\mathbf{p}_s$}node[below,font=\footnotesize]{$ \mathbf{n}_s$} (fsm.west |- surf);
  \draw[arr] (nlf.east)  -- (fsm.west |- nlf);

  % ── FSM → Planning ─────────────────────────────────────────
  \draw[arr] (fsm.east |- exp)   -- node[above,font=\footnotesize]{en.} (exp.west);
  \draw[arr] (fsm.east |- fast)  -- node[above,font=\footnotesize]{en.} (fast.west);
  \draw[arr] (fsm.east |- bpose) -- node[above,font=\footnotesize]{en.} (bpose.west);
  \draw[arrb] (fsm.east |- webui) -- node[above,font=\footnotesize]{state} (webui.west);

  % ── Planning → Control ─────────────────────────────────────
  \draw[arr] (exp.east) -- node[above,font=\footnotesize]{pts.} (wbc.west);
  \draw[arr] ([yshift=-2mm]exp.east) -- ++(0.2,0) |- node[right,font=\footnotesize,pos=0.3]{pts.} ([yshift=2mm]mux.west);
  \draw[arr] ([yshift=2mm]fast.east) -- ++(0.2,0) |- node[right,font=\footnotesize,pos=0.3]{pts.} ([yshift=-2mm]mux.west);
  \draw[arrb] (webui.north) --node[right,font=\footnotesize]{click} (exp.south);
  \draw[arr] (fast.east) -- node[below,font=\footnotesize]{en.} (imp.west);
  % ── WBC ↔ Mux ──────────────────────────────────────────────
  \draw[arr] (wbc.south) -- (mux.north);

  % ── Control → Hardware ─────────────────────────────────────
  \draw[arr] (wbc.east) -| node[xshift=5mm,font=\footnotesize]{$v_x,\omega_z$} (spot.south);
  \draw[arr] (bpose.east) -- node[below,font=\footnotesize,pos=0.6]{$h,\theta$} (spot.west);
  \draw[arr] (mux.east) -- node[above,font=\footnotesize]{(JTC)} (z1.west);
  \draw[arr] (imp.east) -| node[right,font=\footnotesize]{$\boldsymbol{\tau}$ (torque)} (z1.south);

  % ── Column labels ──────────────────────────────────────────
  \node[font=\small\bfseries, anchor=base] at ( 0.0,  3.2) {Sensors};
  \node[font=\small\bfseries, anchor=base] at ( 2.5,  3.2) {Perception};
  \node[font=\small\bfseries, anchor=base] at ( 5.0,  3.2) {FSM};
  \node[font=\small\bfseries, anchor=base] at ( 7.5,  3.2) {Planning};
  \node[font=\small\bfseries, anchor=base] at ( 10,  3.2) {Control};
  \node[font=\small\bfseries, anchor=base] at (12.5,  3.2) {Hardware};

\end{tikzpicture}
