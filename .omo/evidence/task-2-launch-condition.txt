TASK 2 QA — Launch file nlf_skeleton condition (syntax check only, no ROS2)
=============================================================================
RESULT: PASS

Scenario 1 (yolo → no nlf): nlf_skeleton_node has condition=IfCondition("nlf") 
  → When perception_backend:=yolo, condition is false → nlf_skeleton_node NOT launched.
  
Scenario 2 (nlf → yes nlf): 
  → When perception_backend:=nlf, condition is true → nlf_skeleton_node IS launched.

Verification:
  - nlf_skeleton_node with IfCondition + "nlf": PASS
  - yolo_skeleton_node with IfCondition + "yolo": PASS
  - Both conditions use different backends (not copy-paste): PASS
  - Python syntax check (compile): PASS
  - condition parameter is inside Node() constructor (not separate): PASS

Note: ROS2 runtime tests (ros2 launch + ros2 node list) not executed — no ROS2 available.
